#include "general.h"

#include <math.h>

#define min(a,b) (a > b ? b : a)
#define max(a,b) (a > b ? a : b)

int oclDaisySearch(ocl_constructs *, daisy_params *, daisy_params *, float *, int *, int *, int *);
